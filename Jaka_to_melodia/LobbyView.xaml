<UserControl x:Class="Jaka_to_melodia.LobbyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="LightBlue">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TxtWelcome" Grid.Row="0" FontSize="32" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,20,0,5"/>
        <TextBlock x:Name="TxtHighscore" Grid.Row="1" FontSize="20" Foreground="DarkBlue" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <Border Grid.Row="2" Background="White" Margin="150,0,150,20" CornerRadius="10" Padding="10">
            <StackPanel>
                <TextBlock Text="🏆 TABELA WYNIKÓW 🏆" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <ListBox x:Name="ListLeaderboard" FontSize="18" BorderThickness="0" Background="Transparent" HorizontalContentAlignment="Center"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
            <Button x:Name="BtnMusic" Content="🎵 Wybierz folder z muzyką" FontSize="16" Width="220" Height="40" Margin="10" Click="BtnMusic_Click" Background="LightYellow"/>
            <Button x:Name="BtnStart" Content="▶ START GRY" FontSize="18" FontWeight="Bold" Width="180" Height="40" Margin="10" Click="BtnStart_Click" Background="LightGreen"/>
            <Button x:Name="BtnExit" Content="❌ Koniec gry" FontSize="16" Width="150" Height="40" Margin="10" Click="BtnExit_Click" Background="#FF7F7F"/>
        </StackPanel>
    </Grid>
</UserControl>